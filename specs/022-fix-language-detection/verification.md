# 验证记录：语言检测与不中断翻译

**状态**：本轮验收 T045、T049、T050；T001–T002 保持已验收。T046/T051 的检测候选未通过校准，顺序实施停在 T051；US2/US3、完整 Main 链路、真实服务和宿主验收尚未完成。

## 当前环境与前置检查

- 日期：2026-09-11；分支：`fix/language-detection`；基线提交：`501094b4711ce09ef914d39bc97d9596e8b12acc`，本轮改动尚未提交。
- 环境：macOS 26.6.2（25G83）、arm64、Node.js 24.18.0、npm 11.16.0。
- 完整 SDD 轨道；用户通过 `$speckit-implement` 明确授权实施。前置脚本通过，规格质量清单 16/16 已勾选且未修改。
- Git/ESLint/Prettier 忽略配置满足当前项目；私有 npm 包无需新增发布配置。
- `.specify/extensions.yml` 的 `hooks` 为空，实施前后均无钩子。

## 语料与用途

当前版本为 `022-corpus-2026-09-11-2`。全部真值在本轮算法校准前依据完整选段正文审查；没有依据分类输出重标或删除单语失败项。来源、署名、许可、原始字节、选段映射和摘要纳入版本控制；新增材料已暂存以满足加载器的跟踪检查。

| 用途 | 内容 | 作品边界与状态 |
| ---- | ---- | ---------------- |
| 校准 | 303 个自然正样本、12 个负样本；含充分长度的专名/罗马字/无语义负例 | Sintel、Cosmos Laundromat、Tears of Steel、Valkaama；已评估的开发材料 |
| 独立留出 | 21 种语言各 20 个自然正样本，共 420 个；另有 7 个预标负样本 | 《096》及独立原创负例；完整作品与译本不进入开发，**未评估** |
| 混合语义 | 14 个预标用例；包含 55:45、40:35:25、并列、重复、cue 数与文字量不同、人名、译文和罗马字 | 单独版本与分母；已用于开发验证，不能填充留出正样本 |
| 已知回归 | 原冻结清单、正文、同语言 case 与两个指定短轨原样保留 | 两个指定短轨所属 Elephants Dream 未进入校准；七个 FFmpeg 正文仍只允许本地使用 |

校准新增保加利亚语、加利西亚语和挪威语，用于相近语言竞争；马来语另有预标语义用例。英德 Valkaama 选段各 100 cue，实际正文超过 2048 码元，德语超过 4096。留出正样本包含短轨 147、中等轨 252、长轨 21；日语一个样本转换为 ASS 并取代原 SRT，不增加计数。ASS 正文逐字符保持，时间精度差异见[版本说明](../../tests/fixtures/languages/versions/022-corpus-2026-09-11-2/README.md)。

许可见[《096》](../../tests/fixtures/languages/licenses/scp-short-film.md)、[Valkaama](../../tests/fixtures/languages/licenses/valkaama.md)、[Blender](../../tests/fixtures/languages/licenses/blender-open-subtitles.md)和[新增人工边界](../../tests/fixtures/languages/licenses/authored-boundaries-v2.md)。37 项新增原始输入的 Git 索引字节摘要全部吻合；语料范围内的 `.gitattributes` 禁止自动换行转换。

| 清单 | manifestHash |
| ---- | ------------ |
| 用途索引 | `5d481a9599789ea3e09b3425fec289dc85a32f9b5980e70ab4bcb6c22988be54` |
| 校准 | `f0aceed87ef627d6b0418fd78edc74d600aa540fdaa7be2ad00229fb3744d89a` |
| 独立留出 | `ee8a429de897408b8d81ccae797066b87d9416f567a2a3974d2caa7a6f252f09` |
| 混合语义 | `d4dca1ce50c696186bc15cc2a9a8628c00d6e753e8acde7a22413b04389df930` |
| 当前校准报告 | `c7f5b5e52661add956fb27bf61cf169b379ab7e4fec81f40d71bd38386c4ec59` |

## 当前检测候选与校准失败

采样同时检查 64 个有效独立 cue 与 4096 码元上限；超限时覆盖四区域，区域内均分预算，单个超长 cue 保留四段正文。实际扫描重复 cue 也受每批 128 cue 限制，截取不拆开代理对。聚焦回归覆盖 63/64/65 个长 cue、稀疏正文、后段主要语言、单个超长 cue、重复扫描及同一 cue 内的双语行。

候选实现取消 `minimumScriptRatio` 和 `minimumSupport`，按不重叠局部证据的 Unicode 字母数计权。标准 franc 完整候选参与局部及片段上下文排序，脚本只构造证据边界；最多四个上下文片段，每次分类输入不超过 2048 码元。未归属部分不计入赢家，最高权重并列保留未知。**当前归属与可靠性方案仍未通过质量门，不能视为已验收修复。**

最终有界搜索比较 144 组参数，先检查错误可靠率与负例可靠率均 ≤1%、混合预期全部通过，再按作品分组错误率、正确率及固定参数向量排序。满足约束的候选为 **0**，报告状态为 `failed-search`。下表均为开发校准结果，**不是 SC-001 独立留出验收**。

| 配置 | 正样本正确可靠 | 全部样本错误可靠 | 负样本可靠 | 未知 |
| ---- | -------------- | ---------------- | ---------- | ---- |
| 当前生产候选常量 | 240/303 = 79.21% | 14/315 = 4.44% | 0/12 | 61/315 |
| 搜索中约束超额最小的候选 | 227/303 = 74.92% | 8/315 = 2.54% | 0/12 | 80/315 |

失败候选没有写入生产常量，也没有宣称参数冻结。两组参数、完整语言/现象统计、作品分组及搜索规则保存在[校准报告](../../tests/fixtures/languages/versions/022-corpus-2026-09-11-2/calibration-result.json)。不能只通过增加未知输出验收，也不能据此断言标准 franc 在所有方案下都无法达到目标。

验收入口先断言存在合格校准候选；当前在该断言失败，未调用新留出集的生产检测器。用途索引相应保留留出 `unevaluated`，校准及混合语义为 `evaluated`。后续改进可继续使用当前开发材料，完成参数冻结后才进行此留出版本的首次评估；一旦评估后再次调参，须另建独立留出版本。

旧版本 `022-corpus-2026-09-10-1` 的历史结果为正确 353/402、错误 32/415、负例可靠 0/13；两个短轨 2/2、七个本地回归 5/7。它已明确归为已知回归。本轮未重跑九个指定回归，旧结果不代表当前候选通过 SC-002。

## 当前代码验证

| 命令/检查 | 结果 |
| --------- | ---- |
| 语料、源/目标目录、检测、Coordinator、凭据泄漏聚焦测试 | 75 通过、1 个显式校准用例默认跳过；包含语料 contract 16/16、检测单测 32/32 |
| `SUBTANDEM_LANGUAGE_CALIBRATION=1 npm test -- tests/unit/language-detection.test.ts -t calibration` | 失败：0/144 个合格候选；保留失败报告 |
| `npm test` | 787 通过、1 失败、19 跳过；唯一失败为校准前置门，真实服务及本地媒体入口默认关闭 |
| `npm run typecheck`、`npm run lint` | 通过 |
| `npm run build:native`、`npm run test:native` | 通过，退出码 0；三个 helper 双架构构建和签名完成 |
| `npm run build`、`npm run verify:package`、`npm run pack` | 通过，当前安装包已重建及审计 |

Parcel 在沙箱中不能打开 LMDB 缓存；项目原构建命令在沙箱外成功。全量和 native 测试使用本机回环测试服务，没有启动真实付费服务。未调用浏览器控制或 Computer Use。

独立执行 `npm test -- tests/integration/performance.test.ts --maxWorkers=1 --no-file-parallelism`，9/9 通过。总计 30 个独立冷进程、1000 次预热后检测及 25 次 Coordinator 等待；各负载数据如下。所有等待均 ≤500 ms；这些是 Node 检测终态数据，**不能代替 Main 自动准入或 IINA 宿主验收**。

| 负载 | 冷 p95 / ms | 热 p95 / ms | 同步片段 p99 / ms | 最大检测等待 / ms |
| ---- | ----------- | ----------- | ----------------- | ----------------- |
| `maximum` | 39.43 | 14.71 | 0.86 | 97.69 |
| `preprocessing` | 29.62 | 8.23 | 0.25 | 191.26 |
| `shared-script` | 28.91 | 5.61 | 0.96 | 84.39 |
| `high-trigram` | 53.45 | 26.46 | 5.07 | 104.37 |
| `reliable` | 40.42 | 14.84 | 0.89 | 94.23 |

- 当前检测文件 SHA-256：`b84309d0a9df46125b609d4f6e5d051ad4839a50a15f565d9c02bd1cf28344c0`。
- 锁文件 SHA-256：`c5df072dfdafa0b4b2970af43f1ebc7e855cc197fba6669fcba4b6105c7093f6`。
- 本地包版本：`0.1.3`；归档 SHA-256：`547ba6fbd3f457bb25536d10bf428b1e8e18d57c143103cb5ef3dd72698a6a1a`。

包仅用于本轮审计；质量门失败，未安装、未发布、未提升版本，不能作为已验收修复交付。后续任务及各自授权/手动验收边界继续保留在 [tasks.md](tasks.md)。
