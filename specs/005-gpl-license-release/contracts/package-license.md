# 安装包许可合同

正式 `.iinaplgz` 根目录只允许：

```text
Info.json
README.md
LICENSE
THIRD_PARTY_NOTICES.txt
dist/
```

其中 `LICENSE` 与 `THIRD_PARTY_NOTICES.txt` 必须存在、不得为符号链接或加密条目，并与打包输入的仓库根文件一致。`dist/` 的原有运行材料白名单和敏感内容拒绝规则不变。
